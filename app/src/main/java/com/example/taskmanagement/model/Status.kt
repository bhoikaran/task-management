package com.example.taskmanagement.model

/*enum class Status {
     IN_PROGRESS, COMPLETED
}*/



enum class Status {
     IN_PROGRESS,         // Task is being worked on
     MARKED_DONE,         // User has marked it done
     VERIFIED_DONE        // Admin has verified it
}
